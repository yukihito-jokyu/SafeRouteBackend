spring:
  datasource:
    url: jdbc:mysql://localhost:3306/safe_route_db?useUnicode=true&characterEncoding=UTF-8&connectionCollation=utf8mb4_general_ci
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  http:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
mybatis:
  mapper-locations: classpath:mappers/*.xml
